services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      APP_NAME: Survey Paper Analysis
      API_PREFIX: /api
      DATA_DIR: /app/data
      CORS_ORIGINS: http://localhost:8080,http://localhost:5173
      MAX_PDF_CHARS: 0
      DEFAULT_PROVIDER: openai
      DEFAULT_MODEL: gpt-4.1-mini
      REQUEST_TIMEOUT_SECONDS: 300
      OPENAI_BASE_URL: https://api.openai.com/v1
      KI4BUW_BASE_URL: https://llm.ki4buw.de/v1
      KI4BUW_MODELS: openai/qwen3
      KI4BUW_MAX_INPUT_TOKENS: 9000
      KI4BUW_MAX_COMPLETION_TOKENS: 1024
      ANTHROPIC_BASE_URL: https://api.anthropic.com/v1
      DEFAULT_TEMPERATURE: 0.1
    ports:
      - "8000:8000"
    volumes:
      - survey_paper_data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_BASE_URL: /api
    depends_on:
      - backend
    ports:
      - "8080:80"
    restart: unless-stopped

volumes:
  survey_paper_data:
